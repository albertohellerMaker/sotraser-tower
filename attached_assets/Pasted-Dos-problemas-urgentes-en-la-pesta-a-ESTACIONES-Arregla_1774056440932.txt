Dos problemas urgentes en la pestaña ESTACIONES.
Arreglar ambos ahora.

═══════════════════════════════════════════════
PROBLEMA 1 — ELIMINAR ALERTA DE HORARIO
═══════════════════════════════════════════════

En server/geo-routes.ts función analizarCarga():

Buscar y eliminar COMPLETAMENTE cualquier
bloque de código que contenga:

  "horario inusual"
  "Carga en horario"
  esMadrugada
  hora >= 1 && hora <= 4
  hora <= 4
  "AM"
  "horario"

No comentar — BORRAR completamente.
Esta señal no existe para Sotraser.

Verificar que después de borrar
el código compile sin errores.

═══════════════════════════════════════════════
PROBLEMA 2 — OCULTAR CAMIONES SIN ECU
═══════════════════════════════════════════════

En la pestaña ESTACIONES del frontend
(geovalidator.tsx componente EstacionesTab
y dentro de cada EstacionRow):

Los registros que muestran "SIN DATO" en
la columna de ECU no deben aparecer
en la lista principal.

Agregar filtro en el componente:

OPCIÓN A — Filtro por defecto:
  Mostrar solo cargas donde
  tiene_cruce_ecu = true
  O nivel_anomalia !== 'NORMAL'
  
  Es decir: mostrar los que tienen datos ECU
  Y mostrar los que tienen anomalía aunque
  no tengan ECU (para no perder alertas reales)

OPCIÓN B — Toggle en los filtros:
  Agregar botón al lado de los filtros actuales:
  
  [☑ Solo con ECU]
  
  Por defecto: ACTIVADO
  Al desactivar: muestra todos incluyendo sin ECU

Implementar OPCIÓN B porque es más flexible.

El toggle debe:
  - Estar activado por defecto
  - Persistir en localStorage
  - Mostrar el conteo de cuántos se están
    ocultando: "X cargas sin ECU ocultas"

═══════════════════════════════════════════════
VERIFICACIÓN
═══════════════════════════════════════════════

Después de los cambios verificar:

1. Abrir pestaña ESTACIONES
2. Confirmar que NO aparece ningún mensaje
   "Carga en horario inusual"
3. Confirmar que las filas SIN DATO
   están ocultas por defecto
4. Confirmar que el toggle funciona
   para mostrarlas si se necesita

Mostrar los cambios en analizarCarga()
antes de aplicar al frontend.