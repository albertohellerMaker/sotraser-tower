En server/aprendizaje-engine.ts modificar
el ciclo de aprendizaje:

CAMBIO 1 — Ciclo cada 90 segundos:
  En vez de setInterval de 30 minutos,
  usar ciclo de 90 segundos que hace
  solo lo liviano:
  
  CICLO RÁPIDO (cada 90 seg):
    - Leer nuevos puntos GPS de geo_puntos
      de los últimos 3 minutos
    - Detectar si algún camión completó
      un viaje (volvió a base o lleva
      60+ min detenido)
    - Si detecta viaje completado:
      · Cerrar el viaje en viajes_aprendizaje
      · Llamar procesarViajeNuevo(viaje)
      · Actualizar perfil conductor y camión
  
  CICLO PROFUNDO (cada 30 minutos):
    - Recalcular todas las estadísticas
    - Detectar cambios de patrón
    - Actualizar scores
    - Generar alertas si corresponde

CAMBIO 2 — Aprendizaje separado por contrato:

  En conductores_perfil agregar:
    contrato (text) — ya existe
    meta_kmL_contrato (real)
      — promedio de metaKmL de los camiones
        de ese contrato, no un número fijo
  
  En corredores agregar:
    contrato (text) — ya existe
    tipo_operacion (text)
      'DISTRIBUCION' | 'MINERIA' | 'CARGA_GENERAL'
    
    Asignar automáticamente:
    - Si faena contiene 'CENCOSUD' → DISTRIBUCION
    - Si faena contiene 'ANGLO' → MINERIA
    - Otros → CARGA_GENERAL

  El score y las alertas usan la meta
  del CONTRATO, no un umbral global:
  
  En vez de:
    if rendimiento < 2.5 → alerta
  
  Usar:
    meta = camion.metaKmL (configurada por contrato)
    if rendimiento < meta * 0.7 → alerta

CAMBIO 3 — Viaje completado por tipo de operación:

  La lógica de cuándo termina un viaje
  es diferente por contrato:
  
  DISTRIBUCION (Cencosud):
    Viaje termina cuando:
    · Camión regresa a base (geocerca BASE)
    · O lleva 3h+ detenido fuera de base
    Duración típica: 4-16h
  
  MINERIA (Anglo):
    Viaje termina cuando:
    · Camión regresa a base
    · O lleva 8h+ detenido (puede pernoctar en faena)
    Duración típica: 12-36h
  
  Detectar tipo_operacion del camión
  via su faena y aplicar la lógica correcta.

CAMBIO 4 — Log detallado cada ciclo:

  console.log(`[APRENDIZAJE 90s] 
    GPS nuevos: X puntos
    Viajes detectados: X nuevos
    Procesados: CENCOSUD: X | ANGLO: X
    Perfiles actualizados: X conductores, X camiones
    Alertas generadas: X`)

No tocar nada más.
Muéstrame el código del engine modificado
antes de aplicar.